[pytest]
markers =
    unit: Unit tests (no external dependencies — LLM/MCP mocked)
    db: Database tests (requires PostgreSQL)
    integration: Integration tests (requires running services)
    slow: Slow-running tests (real LLM calls)
asyncio_mode = auto
testpaths = tests
